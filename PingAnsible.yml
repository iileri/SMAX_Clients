---
- name: Test Windows Ping
  hosts: all
  tasks:
    - name: Test Ping
      win_ping:
    - name: 0. Windows | Check hostname
      command: hostname
      register: hostname_result
      changed_when: False
      ignore_errors: True  # rc is 3 when firewalld is stopped
      when: ansible_os_family == "Windows"
      tags: hostname_status
    
